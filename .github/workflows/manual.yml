name: build
on: [push]
jobs:
  ubuntu-test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code 
        uses: actions/checkout@v2
      - name: print secret key
        run: |-
          ls
          pwd 
          psudo useradd piriyapong_linux
          
          

          
 
